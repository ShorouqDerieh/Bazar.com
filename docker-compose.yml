services:
  catalog:
    build: ./catalog
    ports:
      - "5000:5000"

  order:
    build: ./order
    ports:
      - "5001:5001"
    depends_on:
      - catalog

  frontend:
    build: ./frontend
    ports:
      - "5002:5002"
    depends_on:
      - catalog
      - order